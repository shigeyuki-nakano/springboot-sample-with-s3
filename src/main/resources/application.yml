spring:
  datasource:
    url: jdbc:mysql://localhost:3306/food_memo
    driverClassName: com.mysql.cj.jdbc.Driver
    username: food_memo
    password: food_memo

  mvc:
    hiddenmethod:
      filter:
        enabled: true
